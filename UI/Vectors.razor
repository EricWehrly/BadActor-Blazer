<CollapsibleMenu MenuName="Infiltration Vectors" MenuNameShort="Vectors" MenuIcon="mdi mdi-ray-start-arrow"
                 MenuClasses="purple" HandleClasses="purple">
    @foreach (InfiltrationVector vector in InfiltrationVector.List)
    {
    <div class="vector">
        @vector.DisplayName
        <!--
        @vector.DisplayName @getCountLabel(vector)
        <span class="btn btn-info">Buy <span class="@coins.Icon"> @vector.Cost</span></span>
    -->
    </div>
    }
</CollapsibleMenu>

@code
{
    private Resource coins = Resource.Get("Coins");

    private string getCountLabel(InfiltrationVector vector)
    {
        return vector.Count + "x";
    }
}
